---
title: 我的第一篇博客！！！
date: 2026-06-12 10:44:17
tags:
---
## 今天做了什么

- 成功在本地搭建了 Hexo 博客,并且推送Github
- 把博客个性化了一下，还为内容添加了图片和代码块
- 另外在unity学习中，学到了一种自定义便捷实例化Multiple的方法(下方会展示)
  → 学习心得见：{% post_link 字典Dictionary使用与引用类型的感悟 %}

## 如何发布一篇博客

1. **新建文章**

   ```bash
   hexo new "文章标题"
   ```

2. **本地预览**

   ```bash
   hexo server
   ```

   然后打开 <http://localhost:4000> 查看

3. **部署上线**

   ```bash
   hexo clean
   hexo generate
   hexo deploy
   ```

   访问 <https://bingtanghulu-lu.github.io>

## 图片示例

{% asset_img example.png 博客截图 %}
> *↑ 图片放到 `source/_posts/我的第一篇博客！！！/example.png`，
> 文章里用 `{% asset_img 文件名.png 描述 %}` 引用*

## 代码块写法

&#96;&#96;&#96; Top名称
(具体代码块内容)
&#96;&#96;&#96;

## 代码块示例

```c#
//自定义便捷加载Multiple图集方法
public class Tool
{
    private static Tool _instance;
    public static Tool Instance => _instance;
    private Tool(){}
    private Dictionary<string, Dictionary<string, Sprite>> dic =
        new Dictionary<string, Dictionary<string, Sprite>>();
    public Sprite LoadMultiple(string multipleName , string spriteName)
    {
        //首先判断传入的大图是否已经被加载过了
        //如果有，则直接加载
        if(dic.ContainsKey(multipleName))
        {
            //判断此大图里是否有这个sprite小图
            if(dic[multipleName].ContainsKey(spriteName))
            {
                return dic[multipleName][spriteName];
            }
        }
        else
        {
            //定义一个储存sprite的字典
            Dictionary<string , Sprite> dicTmp = new Dictionary<string, Sprite>();
            //读取指定的大图里面的sprite小图，存入sprite数组
            Sprite[] spr = Resources.LoadAll<Sprite>(multipleName);
            //将读取的sprite加载入字典
            for(int i =0;i<spr.Length;i++)
            {
                dicTmp.Add(spr[i].name,spr[i]);
            }
            //判断最开始传入的spriteName是不是真的存在，有则返回，反之null
            if(dicTmp.ContainsKey(spriteName))
            {
                return dicTmp[spriteName];
            }
        }
        return null;
    }

    public void ClearInfo()
    {
        dic.Clear();

        Resources.UnloadUnusedAssets();
    }
}
```

```bash
# 终端命令
hexo clean && hexo generate && hexo deploy
```

## 感受

希望自己能够坚持，不以物喜，不以己悲。
